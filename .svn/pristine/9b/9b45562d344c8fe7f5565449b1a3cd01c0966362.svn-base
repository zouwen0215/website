import router from '@/router'
import http from '@/utils/http.js'
import URL from '@/utils/url.js'
const state = {
	status:true,
	tempData:{},
};
const getters = {
	
};
const mutations = {
	managementLogin(state,infor){
		state.status = false
	},
	tempData(state,obj){
		state.tempData[obj.name] = obj.param; 
	}


};
const actions = {

};

export default {
	state,
	getters,
	actions,
	mutations,
}